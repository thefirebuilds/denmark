services:
  app:
    image: ghcr.io/thefirebuilds/denmark:latest
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: "5000"
